# CLAUDE.md

Read this file completely before doing any work. Do not redo anything marked Complete in the Status Table without explicit instruction.

## Working directory

`C:\Users\quail\Documents\ATG-site\` — git checkout of DDApp2025/ddapp2025.github.io, main branch.

## Deployment

GitHub Pages serves `origin/main` directly to aestheticstogo.com. No build step, no Actions. Commit + push = live within minutes.

## Never touch

- `C:\Users\quail\Documents\00-ATG\` — business operations root, not website.
- `C:\Users\quail\Downloads\ddapp2025.github.io*` — archived broken clones.

## Project

Launching ATG Clinical Supplies: a soft-gated catalog of exosomes, post-procedure devices, skincare actives, peptide supplements, PRP/PRF procedural equipment, transdermal peptide patches, and regenerative biologics (Florida/Utah where applicable) at injector pricing. Sub-brand inside aestheticstogo.com. Positions ATG as one clinical platform: Mobile EHR + Clinical Supplies. (Note: AI Simulator capability still exists in the codebase but was hidden from public site 2026-04-29 — do not reintroduce simulator marketing copy without explicit instruction.)

## Hard rules — never violate

- Never use: marketplace, med spa, medspa, DoorDash, store, shop.
- Never use: DivaDash, Diva-Dash, Diva Dash. Former brand. Aesthetics To Go only.
- Never reference: Kelly Wellington, Dr. Deborah Kessler-Hudak, Princess.
- Never reference specific revenue split percentages. Use "industry-leading".
- Never compare ATG to DoorDash. Use Uber if needed.
- Always provide complete files when editing code, not snippets.

## Scope

- Six product categories: Exosomes (topical), Post-Procedure Devices, Skincare Actives, Peptide Supplements, PRP/PRF Procedural Equipment, Transdermal Peptide Patches. Plus regenerative biologics sub-catalog (geo-gated to Florida/Utah initially).
- Nav label for the section: "Clinical Supplies".
- URL: `/clinical-supplies`.
- Soft email gate (regex + disposable blocklist client-side, MX check via Cloudflare Worker calling Check-Mail.org).
- Stripe Payment Links for checkout. No server-side cart. Client-side cart in localStorage.
- Email capture: on successful unlock, POST notification email to info@aestheticstogo.com via Brevo transactional API. No spreadsheet logging.

## Status Table

| # | Prompt | Status | Commit | Date | Deliverable |
|---|--------|--------|--------|------|-------------|
| 1 | Audit + design tokens | Complete | 467d499 | 2026-04-21 | docs/DESIGN_TOKENS.md |
| 2 | Rewrite homepage hero | Complete | [see git log] | 2026-04-21 | index.html |
| 3 | SKU research | Complete | [see git log] | 2026-04-21 | docs/SKU_CATALOG.md |
| 4 | /clinical-supplies landing | Complete | [see git log] | 2026-04-21 | clinical-supplies/index.html |
| 5 | Product detail template | Complete | [see git log] | 2026-04-22 | clinical-supplies/{slug}/*.html |
| 6 | Soft email gate + Worker | Complete | [see git log] | 2026-04-22 | clinical-supplies/gate.js, workers/email-verify/ |
| 7 | Cart + Stripe router | Complete | [see git log] | 2026-04-22 | clinical-supplies/cart.js, clinical-supplies/cart.css |
| 8 | Homepage Clinical Supplies section | Complete | [see git log] | 2026-04-22 | index.html |
| 9 | Global nav/footer/sitemap | Complete | [see git log] | 2026-04-22 | nav + footer on all pages, sitemap.xml, docs/POST_DEPLOY_TODO.md |
| 10 | QA report | Complete | [see git log] | 2026-04-22 | docs/QA_REPORT.md |
| — | Geographic strip — removed Las Vegas references, repositioned as nationwide platform | Complete | [see git log] | 2026-04-28 | All city pages hidden and redirected, legacy index files redirected, all visible text and metadata updated, providerapplication.html updated to 50-state dropdown, builder template updated and detail pages regenerated, QA report at docs/GEO_STRIP_QA_REPORT.md |
| — | AI Simulator hide — removed from public visibility, homepage card replaced with provider image | Complete | [see git log] | 2026-04-29 | /ai-simulator/ page hidden and redirected, homepage card replaced with ATG_in_home.jpg sized to fill column, all visible copy and metadata stripped of simulator references (originals preserved as date-marked HTML/CSS/JS comments), QA report at docs/AI_SIM_HIDE_QA_REPORT.md |
| — | Value Prop Realignment Phase 1 — homepage copy and audience parity | Complete | 4392747 | 2026-05-03 | index.html — wordmark, hero H1/subhead/eyebrow, both above-the-fold CTAs, header CTA, FAQ + schema + OG/Twitter meta updated to Regional Lead Provider / Licensed Providers framing per VALUE_PROP_REALIGNMENT_RULES.md. All Rule 2 forbidden strings cleared from homepage. Modal H2 + cta-band paragraph deferred to Phase 3. Phase 2 pending. |
| — | Value Prop Realignment Phase 2 — structure, provider journey, mobile rendering | Complete | 4375636 | 2026-05-03 | index.html — structural reorganization (3-step Provider Journey replacing 2-step Clinical Flow, Professional Suite reordered to 2 cards with Revenue Protected first, Clinical Supplies demoted from above-the-fold to after Provider Journey + Professional Suite, new Patient Experience section housing the relocated AI Diagnostic card). Hero trust strip rebuilt compliance-first (HIPAA / Encrypted Documentation / Malpractice Integration). Closing CTA + application modal H2 renamed to Regional Lead Provider. Four mobile-rendering bug-class fixes at @720px with dated CSS comments documenting each regression class: (1) grid track sizing — `.how-grid`/`.magazine-grid` use `minmax(0,1fr)` not bare `1fr` so img HTML width attrs don't blow out columns via min-content; (2) flex-item nowrap — `.logo-text` and `.hero-actions` buttons allow wrapping at mobile because Phase 1 grew their strings 60-100% past the implicit nowrap budget; (3) modal centering — flex container properties moved to `.modal.active` (not the base `display:none` rule) plus `inset:0` + `margin:0 auto` defensive triple; (4) modal input sizing — smaller font + tighter padding at mobile so long placeholder strings render with maximum visible characters, placed after base rule for cascade-order correctness. .gitignore: added .tmp-diag/ for retained Playwright/Edge visual regression tooling. Temporary openModal console.log diagnostic + window.atgDiag() helper still shipped — to be removed after real-browser verification. Phase 3 pending. |
| — | Value Prop Realignment Phase 3 — clinical governance, practice economics, value-prop reframe | Complete | 68b40bb | 2026-05-03 | index.html — new Clinical Governance section (eyebrow + heading + intro + 4 institutional-gravity pillars in `.governance-grid`: State-Specific Supervisory Frameworks, Malpractice Insurance Integration, Adverse-Event Protocols, Audit-Ready Documentation, plus state-dependence disclaimer). New Practice Economics section (8-row `<table class="economics-table">` cost-structure comparison Traditional Clinic Role vs. ATG Regional Lead Provider; desktop 3-col table collapses to stacked-card layout at <=720px via td[data-label] pseudo-elements; intro paragraph contains permitted "Six-figure" mention; illustrative-results disclaimer below). Professional Suite card body rewrites: Low-Overhead Practice now "Eliminate the fixed overhead of a clinic footprint" (no clinical real estate / front-desk / marketing dept framing); Revenue Protected body rewritten to launch-period payout retention + transparent post-launch fee schedule disclosure. cta-band paragraph rewritten to enterprise-grade infrastructure framing ("EHR, scheduling, credentialed lead flow, payments, supply access, and clinical governance — provided as part of your Regional Lead Provider agreement"). Header CTA shortened from "Apply as Regional Lead Provider →" to "Apply as Provider →" — header-only; long-form preserved at hero primary CTA, cta-band closing button, and modal H2. New @media(min-width:721px) and (max-width:1280px) header flex-wrap fix: `.header{flex-wrap:wrap}` plus nav font-size shrink + header-cta sizing, with dated CSS comment documenting the same flex-item content-overflow regression class as the earlier mobile fixes. "med-spa" / "Med-Spa" replaced with "traditional clinic" / "Traditional Clinic" throughout Phase 3 content per Rule 2 strict interpretation. Two new utility CSS classes documented inline: .governance-grid (2x2 desktop, 1-col mobile, minmax(0,1fr) for grid track-sizing safety) and .economics-table (responsive table with stacked-card mobile layout). Pre-existing 1281+ viewport header overflow flagged as out-of-scope — not introduced or worsened by Phase 3. Patient Experience section subsequently hidden via comment wrap (separate commit 7093234) pending AI simulator capability decision. Phase 4 pending. |
| — | Value Prop Realignment Phase 4 — image asset brief, alt-text hardening, Rule 2 sweep | Complete | 44f79a5 | 2026-05-03 | New repo-root deliverable `ASSET_BRIEF_PHASE4.md` with three full image specifications (Hero / Practice Management / Revenue-Payout) covering paths, dimensions+2x variants, format (WebP+JPG), Rule-10 wardrobe and setting direction, patient demographic, diversity, forbidden elements, and approved alt text — designed so the live swap is a one-line `src` change once assets are produced. Three placeholder images on the homepage (`ATG_in_home.jpg` hero, `WorkingFromHome.png` Low-Overhead card, `TaxMess.png` Revenue-Protected card) flagged as PENDING REPLACEMENT via dated HTML comments referencing the asset brief, with placeholder alt text staged so the swap is alt-stable. Width/height attributes added to every `<img>` site-wide that lacked them, matching actual intrinsic file dimensions (logo 565x571, hero 1024x1024, BeforeAfter 638x467, before_after.jpg 1024x559, ATG_in_home 1024x1024). loading="eager" on hero + header logos; loading="lazy" on all below-the-fold and modal images. Hero `<figcaption>` retained as the fallback caption layer; magazine-card `.mag-card-body` text retained as fallback caption layer for the three card images. og:image (points to `og-image.jpg`, NOT to `ATG_in_home.jpg`) left unchanged with a dated TODO comment instructing the future swap commit to review og-image and twitter:image alignment with the new hero. Site-wide alt-text sweep (22 secondary HTML files): all logo `<img>` alt strings harmonized to "Aesthetics To Go — mobile clinical operating system for licensed providers."; all logo-tagline `<span>` strings rewritten from the Rule-2-forbidden "The Clinical Platform for Mobile PAs" to "The Mobile Clinical Operating System for Licensed Providers."; two image alts containing "PA-C" (about/index.html, careers/index.html) and three containing AI-simulator references (about, careers, providers) rewritten to Rule-10-compliant "Licensed provider" framing; clinical-supplies builder template `_build-detail-pages.js` updated so future regenerations don't reintroduce the forbidden tagline. Three visible "Uber for aesthetic [care/medicine]" gig-economy comparisons (contact x2, faq x1) plus the FAQ chatbot LLM system prompt rewritten to abstracted directive language ("NEVER use any gig-economy, food-delivery, or ride-share analogy"; "NEVER use storefront-clinic terminology"; "NEVER use online-storefront, retail, or e-commerce language") so the directives still constrain the LLM without containing the literal Rule-2 forbidden substrings. Final repo-wide grep confirms zero matches for all Rule 2 strings (PAs tagline, Dedicated Platform, Mobile Injectors tagline, PA Platform, Mid-Level, DoorDash, Uber for aesthetic, like/Similar to how Uber, white-label, $250k, DivaDash variants, marketplace) AND zero matches for PA-C inside `alt=` or `aria-label=` attribute values. Out of scope and explicitly deferred: (a) PA-C in body copy / meta keywords / JSON-LD (Rule 2 limits PA-C to image alt and aria-label only; Rule 1 retains PAs as #3 in the audience hierarchy), (b) "med spa" body sweep on the 22 non-homepage pages (CLAUDE.md deferred items list, policy decision pending), (c) broader Phase 1-3-style value-prop body-copy realignment on /providers /about /careers /blog /contact /faq /terms /privacy /hair-restoration /clinical-supplies pages (separate effort — header strings only were touched in this phase). |
| — | Value Prop Realignment Phase 5 — /providers/ page realignment to Phase 1-3 voice | Complete | dca6579 | 2026-05-03 | providers/index.html — single-file rewrite. Hybrid structural approach: kept the page's existing section structure (Hero → Feature Strip → Value Grid → Comparison Table → How to Join → Tech Showcase → Social Proof → FAQ → CTA Band → Footer + Modals), added a new Clinical Governance section between the Value Grid and the Comparison Table, stripped all four AI-Simulator commented-out blocks (feature-strip span, value-card, comparison-table row, tech-card) and all four `<!-- prior text 2026-04-29 -->` historical comments. Meta/SEO/schema fully rewritten: `<title>`, meta description (re-trimmed to ~155 chars), keywords (Rule-1 reordered to put physician/NP/PA first, "PA concierge aesthetics" added in follow-up to close audience-parity gap), OG title/description, Twitter card, and JSON-LD FAQPage (Q2 question replaced "Can PAs legally do mobile Botox?" → "Who can practice on Aesthetics To Go?"; all five Q&A answers rewritten to Regional Lead Provider / clinical operating system framing). Top bar matched to homepage ("Regional Lead Provider Applications Open · Limited Territories"); header CTA shortened to "Apply as Provider →". Hero brought to homepage parity: eyebrow "Concierge Aesthetic Provider Network.", H1 "The Six-Figure Private Practice. Zero Overhead. Full Clinical Autonomy. Direct Patient Access." with `<span>` accent on "Six-Figure", subtitle in Rule-1 hierarchy, primary CTA "Apply as Regional Lead Provider →". Feature-strip rebuilt as 6 compliance/governance pills (HIPAA Compliant / Encrypted Documentation / Malpractice Insurance Integration / Clinical Governance Built-In / Wholesale Supply Access / Full Payout Retention*), retiring the prior "Zero Monthly Fees / Zero Merchant Fees" framing per Rule 9. Value grid: all six `<div class="value-card-icon">` emoji elements removed (the CSS rule stays in `<style>` for potential future use), all six cards rewritten — Card 1 to direct-deposit/launch-payout, Card 2 to Mobile COS, Card 3 NEW "Credentialed Patient Lead Flow" replacing the stripped AI-Sim slot, Card 4 to Injector-Priced Supplies, Card 5 to Smart Scheduling (with the prior "AI scheduling engine" wording removed), Card 6 to Clinical Autonomy (1099 IC framing). New Clinical Governance section reuses the existing `.how-it-works` wrapper + `.how-grid` utility (no new utility classes per spec) for 4 pillars (State-Specific Supervisory Frameworks / Malpractice Insurance Integration / Adverse-Event Protocols / Audit-Ready Documentation) plus state-dependence disclaimer, mirroring the homepage Phase 3 governance content. CSS specificity follow-up: the spec's original inline `style="grid-template-columns:repeat(2,minmax(0,1fr))"` on the governance `.how-grid` was a layout dead-end — inline-style specificity beats any selector short of `!important`, so the existing `@media(max-width:720px){.how-grid{grid-template-columns:1fr}}` rule on line 266 could not collapse the grid on mobile (verified 2-col rendering at 375 in Edge). Refactored: inline style removed from the element, replaced with a scoped `@media(min-width:721px){#clinical-governance .how-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}` rule positioned after the existing `@max-width:720px` block in `<style>`. Behavior chain: ≥721px → 2 columns via the new scoped rule; ≤720px → 1 column via the existing rule on line 266. No `!important` used. Provider Journey `#how-to-join .how-grid` remains 3-col at desktop (verified 368px×3 at 1280). Comparison table rewritten: eyebrow "Practice Economics", H2 "Aesthetics To Go vs. Traditional Clinic vs. Other Platforms", 8-row cost-structure tbody (Clinic Rent → Front-Desk → Patient Acquisition → Clinical EHR → Merchant Processing → Supply Cost Markup → Schedule Control → Patient Panel Ownership), footnote replaced with the homepage's Rule-8 illustrative-results disclaimer. How-to-Join section reframed as the Rule-6 three-step Provider Journey (Credentialing & Clinical Onboarding / Patient Acquisition & Provider Match / High-Margin Payouts & Revenue Retention). Tech showcase: AI-Sim card stripped, remaining EHR card body refined to "audit-ready" framing, payments card eyebrow renamed "Direct Deposit, Provider-Retained" with body rewritten to launch-period absorption + RLP agreement framing. Social proof quote replaced (unverifiable "Industry Analysis, 2025 Mobile Medical Services Report" citation → "— Aesthetics To Go editorial"). All six FAQ answers rewritten to Phase 1-3 voice with "operating system" framing replacing prior "operating platform"; Card 5 prior-text comment stripped. CTA band heading + paragraph + button matched to homepage RLP framing; mailto link preserved verbatim. Modal H2 + subtitle + name placeholder ("Jane Doe, MD / NP / PA-C") match homepage. Sign-in modal and `<script>` block unchanged. Final grep on providers/index.html confirms zero matches for: Founding/founding, AI Simulator/AI simulator/diagnostic simulator, Free EHR, Mobile PAs / Mobile Injectors, $250k/$300k, Med-Spa, marketplace, white-label, DivaDash, DoorDash, Uber-for. PA-C count = 1 (form placeholder only, intentional). Audience-parity ratio: Regional Lead Provider + Licensed Provider mentions (≈23) substantially outnumber PA mentions (4, all in Rule-1 lists "physicians, NPs, and PAs"). Pre-existing /providers/ header overflow at every viewport (375/768/1024/1280/1440) flagged as out-of-scope — caused by `.logo-text{white-space:nowrap}` + Phase 4 longer wordmark tagline + horizontal nav with no flex-wrap; Phase 2/3 mobile-rendering fixes from `index.html` were never ported to `providers/index.html` (separate `<style>` block per page). Baseline measurement (git stash + render) confirmed identical 583px body width at 375 BEFORE Phase 5 edits, so Phase 5 introduced zero new overflow. Phase 6 cross-page sweep is the natural place to port the mobile-rendering fixes to /providers/ and the other 10 secondary pages. Out of scope and explicitly deferred to Phase 6: title-tag/meta-description/top-bar/header-CTA/audience-string/Founding-Provider sweep on /about /careers /contact /faq /blog /botox /fillers /hair-restoration /terms /privacy. Phase 6 pending. |
| — | Value Prop Realignment Phase 6A — cross-page parity audit | Complete | (uncommitted; report in `docs/PHASE6A_AUDIT_REPORT.md`, gitignored) | 2026-05-03 | docs/PHASE6A_AUDIT_REPORT.md (1,152 lines, gitignored) — read-only static + Playwright (Edge) audit of the 10 secondary pages (`/about/`, `/careers/`, `/contact/`, `/faq/`, `/blog/`, `/botox/`, `/fillers/`, `/hair-restoration/`, `/terms/`, `/privacy/`). Per-page entries cover: page identity + SEO surface (title / meta / OG / Twitter / JSON-LD inventory / H1 + H2 list), forbidden-strings audit per Rule 2, audience-parity verdict per Rule 1, voice + brand parity verdict per Rule 4 / Rule 9, mobile-rendering check (CSS rule presence + Playwright at 375 / 768 / 1024 / 1280), per-page recommendation (NO-CHANGE / LIGHT-TOUCH / MEDIUM-REWRITE / FULL-REPLACEMENT), SEO-specific notes. Top-of-report executive summary covered overall realignment state, top 5 cross-cutting issues, recommended Phase 6B scope (CC recommended Tight → Albert chose Modified-Tight + 3 named exceptions), per-page summary table, SEO consistency findings, mobile-fix porting plan (Option 1 — per-page paste), and a single most surprising finding (`/blog/` AI Simulator article that escaped the 2026-04-29 hide pass — flagged for resolution before Phase 6B). Driven the Phase 6B execution prompt at `PHASE6B_PROMPT_FINAL.md` (gitignored). Phase 6B pending. |
| — | Clinical Supplies hide — removed from public visibility, all assets preserved for restoration | Complete | [see git log] | 2026-05-05 | 11 Clinical Supplies pages converted to clean redirect stubs (originals in git history); homepage feature section, nav links, footer rows wrapped in HTML comments with date marker 2026-05-05; sitemap.xml entries wrapped; providers value-card and comparison row hidden; FAQ chatbot LLM system-prompt bullets at lines 813 + 833 deleted (HTML comments inert inside JS template literal — see QA report Issue 2); QA report at docs/CLINICAL_SUPPLIES_HIDE_QA_REPORT.md; restoration notes in docs/CLINICAL_SUPPLIES_HIDE_INVENTORY.md. Cloudflare Worker, Brevo, Check-Mail, catalog code (catalog.js, gate.js, cart.js, cart.css, _build-detail-pages.js), and builder template all preserved untouched on disk. Intentional retentions: `blog/index.html:762` generic dictionary usage of "clinical supplies" (needles, cannulas, gauze); `faq/index.html:152` JSON-LD answer "wholesale supply access on Botox, fillers, and clinical supplies"; `careers/index.html:620` benefit-card body and `about/index.html:554` platform-card body (both generic dictionary usage); "Wholesale Supply Access" / "Wholesale Supply Pricing" feature labels across providers/careers/about. Note: Prompt 1 inventory mislabeled the generic dictionary usage as `botox/index.html:760` — actual content is on `blog/index.html:762`; net functional outcome correct (intent was retention). |
| — | Careers consolidation — content migrated to /providers/, careers URL hidden as redirect stub | Complete | [see git log] | 2026-05-08 | Migrated careers Roles section (3 cards: MD/DO Physician, NP, PA-C with credentials, body, ✓ bullet lists, Apply Now buttons), Qualifications section (6 numbered cards: Active License / Aesthetic Experience / Malpractice / Supervisory / Professional Standards / Patient-First), and Stats section (4 cards: $0 Monthly / $0 Merchant / 100% Revenue / 48hr Credentialing) to providers/index.html. Added new 2-card Training section ("Train With Expert Injectors": Hands-On Injection Training + Online Curriculum) between Roles and Qualifications. Added 7th FAQ card ("Where are you currently recruiting?") to providers FAQ section + corresponding 6th mainEntity to FAQPage JSON-LD schema. Added hidden `_subject="Providers Page — Provider Application"` field to Provider Application Modal for Formspree subject-line routing. New providers section order: Hero → Feature Strip → Value Grid → Roles (NEW) → Training (NEW) → Qualifications (NEW) → Clinical Governance → Comparison Table → Stats (NEW) → How to Join → Tech Showcase → Social Proof → FAQ (now 7 cards) → CTA Band. Hidden Careers link in `_templates/nav.html`, `_templates/mobile-nav.html`, and `_templates/footer.html` with date marker 2026-05-08; ran `node _inject-templates.js` to propagate to 11 pages (careers excluded since now a stub). Removed `'careers/index.html': '/careers'` entry from ACTIVE_PATH_MAP and removed `'careers/index.html'` from PAGES array in `_inject-templates.js`. Converted careers/index.html to clean redirect stub (15 lines, ~470 bytes) pointing to /providers/ — original 986-line page preserved in git history. Repointed 7 inline /careers links across about/blog/contact ×3/privacy/terms to /providers, with link-text rewrites where appropriate (Career Opportunities → Provider Opportunities, View Open Positions → For Providers, careers page → providers page, Careers at Aesthetics To Go → For Providers). Fixed pre-existing privacy/index.html and terms/index.html header CTA bug — button labeled "Apply as Provider" was navigating to /careers; corrected to /providers. Wrapped /careers/ URL block in sitemap.xml in XML comment with date marker 2026-05-08. JSON-LD JobPosting schema dropped with the careers stub (intentional — schema represented an active job listing, no place for it on the consolidated page). Lifestyle, Mission, Benefits/Value grid, Onboarding, Feature strip, and Hero from careers were intentionally NOT migrated (overlapped with providers content or duplicated existing sections). Providers hero ("Take your aesthetic practice mobile - zero overhead, full autonomy, direct patient access.") preserved unchanged. |
| — | Value Prop Realignment Phase 6B — cross-page parity execution (10 secondary pages) | Complete | f5b6301 / 709d112 / c967be8 | 2026-05-04 | 10 secondary pages brought into Phase 1-5 voice via three group commits (Group 1 patient-facing, Group 2 provider-recruitment, Group 3 high-stakes) plus this Status Table commit. Modified-tight scope per audit + 3 named exceptions executed: (a) /blog/ Uber/Airbnb body paragraph rewritten per spec 2C.19, (b) /careers/ role-card section structurally reordered from PA-C → NP → Physician (Rule 1 inverse) to Physician → NP → PA-C with full credential names + body refresh per spec 3A.16-17, (c) /blog/ JSON-LD med-spa-cost-vs-mobile-practice URL slug investigated per 2C.6 — URL did not resolve to an actual page (verified `ls /blog/` returned only index.html + logo), so the broken BlogPosting JSON-LD entry AND the matching body article-card "The Real Cost of Running a Med Spa vs. Going Mobile" both removed in one operation; deletion documented inline at the deletion site. Title/meta/OG/Twitter unified to canonical "[Topic] | Aesthetics To Go — Mobile Clinical Operating System for Licensed Providers" pattern across recruitment-facing pages (homepage-canonical title pattern); patient-page titles preserved per spec; legal-page titles preserved per spec. All 10 top bars replaced with canonical `Regional Lead Provider Applications Open · Limited Territories`. Provider-recruitment header CTAs unified to `Apply as Provider →` (homepage canonical); patient-page header CTAs preserved (with one judgment call flagged in Group 1 commit message — `/botox/` + `/fillers/` had legacy `Join the Network` → `/providers` redirect-style CTAs which the audit had described as patient-Book CTAs; preserved per spec 1A.10/1B.10 directive `DO NOT change to Apply as Provider →`); legal-page header CTAs unified to `Apply as Provider →` for site consistency. Modal H2 / subtitle / multi-credential placeholder canonical on all 5 provider-recruitment-modal pages (about / careers / contact / faq / blog); patient modals preserved (different placeholder schema). FAQPage schemas rewritten on /about/ (4 Q&A — Q1 Uber comparison stripped + Q2 question rewrite "med spa" → "clinic"), /careers/ (6 Q&A), /faq/ (19 Q&A — including the named-exception Uber comparison strip in Q18 + Q18 question rewrite "med spa" → "clinic"), /blog/ (5 Q&A — Q5 "What is a founding provider?" → "What is a Regional Lead Provider?"), /terms/ (5 Q&A preserved — already legal-context-clean per audit), /privacy/ (5 Q&A preserved per spec 3D.3). JobPosting schema on /careers/ retitled `Founding Mobile Aesthetic Provider (PA-C, NP, or MD/DO)` → `Regional Lead Provider — Mobile Aesthetic Practice` with full description / qualifications rewrite to Rule 1 + RLP agreement framing; datePosted/validThrough/employmentType/hiringOrganization preserved per spec. Blog schema description + 9 BlogPosting items (10 minus the deleted med-spa one) rewritten — all PA-first / "Injectors" primary-label / founding-provider headlines reframed; URL/image/author/datePublished preserved per spec 2C.7. MedicalProcedure / Service descriptions on /botox/, /fillers/, /hair-restoration/ reordered to Rule 1 parenthetical credentials. Phase 2 + Phase 3 mobile-rendering CSS block ported to all 10 pages via shared CSS block in each page's existing inline `<style>`, values aligned with the live homepage `<style>` block. Three named exceptions handled cleanly. /blog/ also received a sitewide one-pass replacement per spec 2C.25 — `founding provider(s) → Regional Lead Provider(s)` and `med spa(s) → traditional clinic(s)` (case-preserved) — applied to the entire page including long-form article body prose since these are mechanical token swaps rather than rewrites; full audience-hierarchy realignment of long-form article paragraphs deferred to Phase 7 per spec preamble. Verification suite (V1-V11 per spec): V1 forbidden-strings sweep clean modulo intentional matches (/about/ 1 founding + 1 med-spa in story-card prose deferred per spec preamble, /careers/ 1 founding inside my own Phase 6B HTML comment documenting the role-card reorder, /faq/ 6 founding inside the Phase-7-deferred LLM `<script>` block, /terms/ 1 founding in §4 spec-protected legal language); V2 top-bar consistency 10/10 OK; V3 modal-canonical 5/5 provider-recruitment pages OK + 3/3 patient pages MISMATCH on multi-credential placeholder (expected — patient modals use different placeholder schema, not in spec scope); V4 Playwright bodyW=innerW=OK at 375 on all 10 pages; V5 JSON-LD validity all blocks parse cleanly across 10 pages (3 blocks /about/, 3 /careers/, 1 /contact/, 2 /faq/, 3 /blog/, 4 /botox/, 4 /fillers/, 4 /hair-restoration/, 2 /terms/, 2 /privacy/); V6 /careers/ role-card visual order Physician → NP → PA-C verified at 1280 via Playwright DOM traversal; V7 /blog/ exception checks pass (AI Simulator article comment-wrapped, Uber + Airbnb 0 matches, med-spa-cost URL removed); V8 /careers/ JobPosting schema title/description/qualifications all RLP-framed; V9 /faq/ LLM `<script>` content byte-identical to HEAD modulo Windows CRLF (autocrlf checkout artifact, not a content change); V10 /terms/ §4 binding zero-fee language preserved at line 327; V11 git status working tree clean (modulo pre-existing untracked planning docs). V12 production smoke test deferred to post-Status-Table-commit per spec. Body rewrites and FAQ chatbot LLM system prompt deferred to Phase 7. /terms/ §4 binding zero-fee legal language pulled out of scope, routed to separate legal counsel review. Pre-existing `og-image.jpg` / hero image swap and Phase 4 ASSET_BRIEF deliverables remain pending photographer/AI image generator. Phase 7 (long-form blog body realignment + LLM system prompt rewrite + /terms/ §4 legal review) is the natural follow-on. |

## End-of-prompt rule

After finishing any prompt 2–10: update this file's Status Table (mark Complete, add commit hash and YYYY-MM-DD date), commit CLAUDE.md with the deliverable.

---

## Post-launch tasks (in order)

Prompts 1–10 are complete. The following tasks run in strict sequence — each depends on the previous being complete. Do not run out of order; doing SEO before the catalog is fully populated would require redoing the work after every expansion.

### Task A — Phase 2 SKU Expansion (Peptide Supplements + PRP/PRF + Transdermal Patches + Regenerative Biologics)

Expand the catalog from 10 SKUs to approximately 30 by adding four new categories researched in a separate Claude chat session. The research prompt is stored locally at `C:\Users\quail\Downloads\Phase2_SKU_Research_Prompt.md` (or similar path). Output is a Markdown file at `docs/PHASE2_SKU_CATALOG.md` with SKU-level detail matching the format of `docs/SKU_CATALOG.md`.

**Scope of Task A execution:**

1. Run the Phase 2 research in a fresh Claude chat. Save output to `docs/PHASE2_SKU_CATALOG.md`.
2. Update `clinical-supplies/catalog.js` to add the new SKUs.
3. Regenerate the 10 existing detail pages and create new detail pages for each new SKU using the existing `_build-detail-pages.js` builder template.
4. Update `clinical-supplies/index.html` to display the new category tabs and any new bundles.
5. Update `sitemap.xml` with the new detail-page URLs.
6. Update `robots.txt` if category-specific crawl rules are needed (likely not).
7. For the Regenerative Biologics category (Florida/Utah only), implement geo-gating or explicit UI labeling so providers outside those states understand the restriction. Design decision required at that step.
8. Commit and push. Update Status Table with a new "Task A" row marked Complete.

Ask Claude in chat: "Run Task A from CLAUDE.md — Phase 2 SKU expansion using the research output at docs/PHASE2_SKU_CATALOG.md."

### Task B — Reseller / distributor account setup playbook

Only after Task A is complete and all Phase 2 SKUs are in the catalog. A step-by-step guide for establishing professional wholesale accounts with every manufacturer across the combined catalog, in priority order. The Phase 2 research output from Task A will include a merged priority list combining the original 9 manufacturers with the new Phase 2 manufacturers.

Covered manufacturers include but are not limited to:

1. BENEV — ERC exosome serum
2. SkinCeuticals Professional — CE Ferulic
3. ZO Skin Health Professional — Daily Power Defense
4. Obagi Professional — Professional-C Serum 20%
5. Dr. Pen authorized US distributor — A20, H6 microneedling pens
6. Allergan Aesthetics — required gateway for SkinMedica TNS Advanced+
7. AnteAGE Professional — MDX Exosome Solution
8. Celluma — PRO LED device
9. Dp Dermaceuticals (Dermapenworld) — EXO-SKIN take-home serum
10. Phase 2 additions from Task A research output (peptide supplement brands, PRP/PRF equipment manufacturers, patch brands, ZEO ScientifiX, etc.)

Each entry in the playbook covers: application URL, credentialing requirements, expected approval time, minimum orders, cold-chain logistics if applicable, and any known gotchas (e.g., Allergan gating SkinMedica, ZEO exclusive MOU implications, state-specific restrictions).

Ask Claude in chat: "Task A complete. Give me the complete reseller account setup playbook per Task B in CLAUDE.md."

### Task C — SEO parity sweep for /clinical-supplies/*

Only after Task B is complete and all catalog SKUs are finalized. Bring every new page under `/clinical-supplies/*` up to the same SEO standard as the existing site pages (the ones graded by Albert's SEO evaluation app). Target: preserve the site's 100% SEO score across every new URL.

**Why this is last:** every SKU added in Task A becomes a new URL that needs SEO treatment. Running SEO before Task A completes guarantees redoing the work. Running it after Task B completes ensures it captures every final SKU page with no future rework.

The sweep covers, per page:

- Unique, keyword-aware `<title>` (under 60 chars)
- Unique `<meta name="description">` (140–160 chars)
- Canonical URL via `<link rel="canonical">`
- Open Graph tags: `og:title`, `og:description`, `og:type`, `og:url`, `og:image`, `og:site_name`, `og:locale`
- Twitter card tags: `twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`
- JSON-LD structured data appropriate to the page type:
  - `/clinical-supplies/` landing: `OfferCatalog` with every SKU
  - `/clinical-supplies/{slug}/` detail pages: `Product` with offer, brand, category, and `Breadcrumb` linking back to the landing
- Single H1 per page, proper H2/H3 hierarchy
- `alt` text on every image (including placeholder tiles until real imagery is licensed)
- `sitemap.xml` updated with every new URL, proper `lastmod` / `priority` / `changefreq`
- `robots.txt` confirmed to allow crawling of `/clinical-supplies/`
- Internal linking from the homepage, footer, and nav to the new section
- Reasonable `Cache-Control` behavior (static site default is fine — just verify nothing is marked `no-cache` unnecessarily)
- Lighthouse SEO and Best Practices checks clean on every new page
- Re-run Albert's SEO evaluation app against a representative sample (landing + 2–3 detail pages per category). Target 100%.

Ask Claude in chat: "Task B complete. Run the SEO parity sweep per Task C in CLAUDE.md — compare new pages against the existing-site SEO baseline and give me a list of gaps with fixes."

---

## Deferred items (not blocking launch)

- 31 occurrences of "med spa" / "medspa" across non-homepage production pages (about/, blog/, faq/, careers/, las-vegas/, henderson/, summerlin/, boulder-city/). Homepage already cleaned 2026-04-22. Policy decision pending on whether to sweep the rest (replace with "traditional clinic" / "brick-and-mortar practice") or allow contrast-usage against competitors.
